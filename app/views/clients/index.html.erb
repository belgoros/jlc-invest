<%= provide(:title, 'All clients') %>
<h1>All clients</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @clients %>
</ul>

<%= will_paginate %>

<%= link_to 'New client', new_client_path %>
