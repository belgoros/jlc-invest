<% provide(:title, t(:edit, model: @admin.class_name)) %>
<h1><%= t(:edit, model: @admin.class_name) %></h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@admin) do |f| %>
        <%= render 'fields', f: f %>

        <%= f.submit class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
