<% provide(:title, "Edit operation") %>
<h1>Update operation</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for([@client, @operation]) do |f| %>

        <%= render 'fields', f: f %>

        <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
    <%= link_to 'Back to List', client_path(@client) %>
  </div>
</div>
