language: ruby
rvm:
  - "2.4.0"
script:
    - bundle exec rake db:migrate
    - bundle exec rspec
    - dist: trusty
    - sudo: false
before_script:
  - psql -c 'create database jlc_invest_test;' -U postgres
